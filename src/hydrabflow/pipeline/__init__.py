"""Fixed infrastructure: adapter/workflow construction, dataset IO, checkpointing, and the five
run stages (simulate, train, evaluate, evaluate_real, tune). End users should not need to edit
anything in this package.
"""
