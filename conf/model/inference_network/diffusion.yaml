# Diffusion posterior network. Expressive; typically more sampling steps than flow matching.
defaults:
  - base_inference_network

type: diffusion
mlp_depth: 4
mlp_width: 128
dropout: 0.05
time_embedding_dim: 32
