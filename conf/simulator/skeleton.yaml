# Placeholder forward model. Running the pipeline with this simulator raises a clear
# NotImplementedError pointing you to src/hydraflow/simulators/skeleton.py. Copy this file to
# conf/simulator/<your_name>.yaml, set `name` to your registered simulator, and add params.
defaults:
  - base_simulator

name: skeleton
params:
  # Example fields your simulator's constructor might read. The skeleton ignores them.
  n_dim_parameters: 2
  n_dim_observable: 2
